name: WebProof AI Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "================================"
          echo "WEBPROOF AI ACTIONS ÇALIŞIYOR"
          echo "================================"
          echo "GitHub Actions bağlantısı başarılı."
