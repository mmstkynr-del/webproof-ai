name: WebProof AI BBC Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: BBC Test
        run: |
          echo "BBC TEST BASLADI"
          python -c "import urllib.request; print(urllib.request.urlopen('https://www.bbc.com/turkce', timeout=30).status)"
          echo "BBC TEST TAMAMLANDI"
