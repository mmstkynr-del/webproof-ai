name: WebProof AI Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "WebProof AI test sunucusu çalışıyor!"
          python --version
